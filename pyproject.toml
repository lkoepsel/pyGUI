[project]
name = "pygui"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "guizero>=1.6.0",
]

[tool.uv]
python-preference = "only-managed"
